"""Per-stage loop instances (placeholder).

Each stage will declare: an input state query, a step function, and a retry
policy (ARCHITECTURE.md section 3). Nothing here is implemented until the
Part 2 adapters design lands; the loop engine itself lives in
:mod:`runtime.loop`.
"""
