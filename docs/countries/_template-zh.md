# {国家名}（{ISO3}）数据源说明

> **读者定位（2026-08-30 定）**：第一读者是**外部开源用户**（未参与本项目的读者）——内部施工复用同一份，不维护两版。要避免的是内部痕迹，不是技术深度。
> **语言七规矩**（写作时遵守）：中文叙述、代码保留原文；每个术语首现即用白话定义，缩写首现必须写全称（中文名+英文全称+缩写），之后方可用缩写；一切举例用真实数据（编号/日期/字节数从库里查出写死，不编造）；表格只装枚举型信息，过程用编号步骤；一切数字带日期出处（如"2026-08-25 实测"）；**内部工序词**（任务书、方案、确认门、施工图、探查/方案/施工等阶段代号、裁决、回弹规则、agent、用户指示等）**与内部路径**（docs/tasks/… 等不随仓库公开的内容）**不得出现在正文**；假定读者只会用命令行、对本项目一无所知。
> **文件套件（结题验收项）**：每国必交 `overview-zh.md` 总览（骨架见下）+ 每源一文件 `{source}-zh.md`（九节骨架见下，不得缺节，不适用也要写明原因）；**文末固定块（必填）**：`*更新日期：YYYY-MM-DD；数据快照：YYYY-MM-DD；数据由{哪次运行/窗口}背书。*`
> **交稿自检**：□ overview 已交 □ 九节齐全 □ 文末固定块已填 □ 正文无内部词与内部链接 □ 每个数字带日期且可从 state.db 复查。
> 英文版归开源侧统一补写，**不是采集任务的验收项**；英文骨架：[_template-en.md](./_template-en.md)。

## 0. overview 总览文件骨架（每国一个，先写）

```
# {国家名}（{ISO3}）数据源总览
> 一个源一个文件：本文件只做总览与共享信息，各源细节见对应文件。
> 文件命名规则：`{iso3}/{source}-{lang}.md`（国家文件夹 + 语言后缀，lang ∈ zh/en）。

## 1. 源清单        ← 表：源 | 覆盖什么 | 数据从哪来 | 说明文件链接
## 1b. 三结构现状    ← 三句：时间序列（必答，缺什么写为什么）/ 决策过程（有→在哪；无→记缺）/ 版本序列（同左）
## 2. 共享访问准备   ← 数据目录/密钥/命令习惯/限速节奏（各国相同的部分）
## 3. 尚未覆盖的政策层 ← 表：层 | 现状（一句话）
（文末固定块）
```

## 1. 源概览

（这个国家的数据从哪来：官方机构、网址、数据的法律地位；覆盖范围与关键数字——共多少政策、跨什么年份；数据是 API 还是网页、正文是否内联。）

## 2. 访问准备

（要不要 API key：要 → 去哪注册（附链接）、限额多少（多少请求/小时）、`.env` 里配哪个变量名；不要 → 写明"无需 key"。有无反爬、需要什么传输方式。）

## 3. 抓什么：任务类型清单

（这是"API 怎么用、有哪些端点"的正面回答。一张表：任务类型 → 请求什么（端点/页面）→ 每次产出什么。）

| 任务类型 | 请求 | 产出 |
|---|---|---|
| … | … | … |

## 4. 数据落到哪

（领域表逐列说明（有哪几张表、每张表记什么）；documents 表承担什么；文件夹布局图——一个政策的文件夹长什么样。）

## 5. 完整案例走查

（用一个真实政策把全部数据从头走到尾：台账行、大事记、文本版本、关联投票，每个数字都能从 state.db 复查出来。读者看完这节就"看见"了整个数据集。）

## 6. 怎么跑

（可直接复制运行的命令：首次采集 / 小规模试跑 / 增量同步 / dry-run / 状态查询 / 修复通道。）

## 7. 更新与增量

（同步游标怎么走、什么键值；政策什么时候算"终局"不再重访；重开规则——源站更新信号如何触发重抓。）

## 8. 已知边界与缺口

（把"坑"写在明处：哪些数据源没有、哪些推迟了、空响应什么情况是正常的、反爬情况。）

## 9. 端点速查表

（附录一：在用的端点全列。附录二：源里还有但暂未用的端点/信息菜单——每项一句话研究价值。）

---

*更新日期：YYYY-MM-DD；数据快照：YYYY-MM-DD；数据由{哪次运行/窗口}背书。*
